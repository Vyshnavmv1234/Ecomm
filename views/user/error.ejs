<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>WestLane</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Bootstrap CSS -->
<link 
  href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
  rel="stylesheet">


  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      color: #222;
      background: #fff;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    img {
      width: 100%;
      display: block;
    }

    /* ---------------- HEADER ---------------- */
    header {
      border-bottom: 1px solid #eee;
      padding: 18px 60px;
    }

    .header-top {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 22px;
      font-weight: 500;
    }

    .search {
      flex: 1;
      margin: 0 40px;
    }

    .search input {
      width: 100%;
      padding: 10px 14px;
      border: 1px solid #ddd;
      font-size: 14px;
    }

    .header-icons {
      display: flex;
      gap: 18px;
      font-size: 14px;
    }

    nav {
      margin-top: 16px;
      display: flex;
      justify-content: center;
      gap: 28px;
      font-size: 14px;
    }

    /* ---------------- HERO ---------------- */
    .hero {
      padding: 40px 60px;
    }

    .hero img {
      max-height: 650px;
      object-fit: cover;
    }

    /* ---------------- EXPLORE MORE ---------------- */
    .explore {
      text-align: center;
      margin: 40px 0 20px;
      font-size: 14px;
    }

    /* ---------------- PRODUCT GRID ---------------- */
    .product-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 30px;
      padding: 0 60px;
    }

    .product-card {
      text-align: center;
      font-size: 13px;
    }

    .product-card p {
      margin-top: 10px;
      color: #555;
    }

    /* ---------------- SECTION TITLE ---------------- */
    .section-title {
      padding: 60px 60px 20px;
      font-size: 14px;
      letter-spacing: 1px;
      color: #777;
    }

    /* ---------------- FOOTER INFO ---------------- */
    .info {
      background: #f7f7f7;
      padding: 60px;
      text-align: center;
    }

    .info h3 {
      font-family: 'Playfair Display', serif;
      font-weight: 400;
      margin-bottom: 14px;
    }

    .info p {
      max-width: 600px;
      margin: 0 auto 40px;
      font-size: 14px;
      color: #666;
    }

    .info-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 30px;
      font-size: 13px;
    }

    /* ---------------- FOOTER ---------------- */

    footer h4 {
      margin-bottom: 14px;
      font-weight: 500;
    }

    footer ul {
      list-style: none;
    }

    footer li {
      margin-bottom: 8px;
      color: #555;
    }
    .store-buttons {
  display: flex;
  gap: 10px;
  align-items: center;
}

.store-badge {
  height: 36px;   
  width: auto;
}


    /* ---------------- RESPONSIVE ---------------- */
    @media (max-width: 900px) {
      .product-grid,
      .info-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      footer {
        grid-template-columns: 1fr;
      }

      header {
        padding: 20px;
      }

      .hero,
      .product-grid,
      .section-title {
        padding-left: 20px;
        padding-right: 20px;
      }
    }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header>
    <div class="header-top">
      <div class="logo">WestLane</div>

      <div class="search">
        <input type="text" placeholder="What are you looking for?" />
      </div>

      <div class="header-icons">
        <span>Sign in</span>
        <span>♡</span>
        <span>👜</span>
        <span>☰</span>
      </div>
    </div>

    <nav>
      <a href="#">All</a>
      <a href="#">Tshirt</a>
      <a href="#">pant</a>
      <a href="#">Old Money</a>
      <a href="#">OverSized</a>
    </nav>
  </header>

<!-- MAIN CONTENT -->
<main class="container text-center my-5">

  <p class="page-title mt-5">PAGE NOT FOUND</p>

  <p class="mt-4 text-muted">
    We can't find the page you <br>
    looking for, it will return to the
  </p>

  <a href="/user/homepage" class="home-btn">
    ← HOME PAGE
  </a>

</main>

<!-- FOOTER -->
<footer class="container border-top pt-5 pb-4 mt-5">
  <div class="row">

    <div class="col-md-4">
      <div class="logo mb-3">WestLane</div>
      <div class="d-flex gap-3">
        <span>📘</span>
        <span>📸</span>
        <span>▶️</span>
        <span>🐦</span>
      </div>
    </div>

    <div class="col-md-4">
      <h6>SHOP</h6>
      <ul>
        <li>Woman</li>
        <li>Man</li>
        <li>Kids</li>
        <li>Home</li>
      </ul>
    </div>

    <div class="col-md-4">
      <h6>SITES & STORES</h6>
      <ul>
        <li>About Us</li>
        <li>Contact Us</li>
        <li>Terms and Condition</li>
      </ul>
    </div>

  </div>
</footer>

</body>
</html>
